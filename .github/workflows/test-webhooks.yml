name: Test Webhooks

on:
  workflow_dispatch:
    inputs:
      test_slack:
        description: 'Test Slack webhook'
        required: false
        default: true
        type: boolean
      test_teams:
        description: 'Test Teams webhook'
        required: false
        default: true
        type: boolean

jobs:
  test-webhooks:
    runs-on: ubuntu-latest
    steps:
      - name: Test Slack Webhook
        if: ${{ inputs.test_slack }}
        uses: 8398a7/action-slack@v3
        with:
          status: success
          channel: '#deployments'
          text: |
            🧪 **Teste de Webhook Slack**
            
            ✅ Webhook configurado com sucesso!
            📅 Data: ${{ github.event.head_commit.timestamp }}
            👤 Triggered by: ${{ github.actor }}
            🔗 Workflow: ${{ github.workflow }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Test Teams Webhook
        if: ${{ inputs.test_teams }}
        uses: skitionek/notify-teams@v1
        with:
          webhook_url: ${{ secrets.TEAMS_WEBHOOK_URL }}
          message: |
            {
              "text": "🧪 **Teste de Webhook Teams**\n\n✅ Webhook configurado com sucesso!\n📅 Data: ${{ github.event.head_commit.timestamp }}\n👤 Triggered by: ${{ github.actor }}\n🔗 Workflow: ${{ github.workflow }}"
            }

      - name: Success Message
        run: |
          echo "✅ Webhooks testados com sucesso!"
          echo "📱 Verifique as notificações no Slack e Teams"
          echo "🔗 Workflow URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" 